# e-Fatura GIB Credentials
# Turkish Tax Authority (GIB) e-Invoice system credentials

# GIB Username (Kullanıcı Adı)
GIB_USERNAME=your_gib_username

# GIB Password (Şifre)
GIB_PASSWORD=your_gib_password

# GIB Environment (Ortam)
# Options: "test" for test environment, "production" for live environment
# Seçenekler: Test ortamı için "test", canlı ortam için "production"
GIB_ENVIRONMENT=test

# Optional: Company Tax ID (Vergi Numarası - opsiyonel)
# GIB_TAX_ID=1234567890

# Optional: Company Name (Firma Adı - opsiyonel)
# GIB_COMPANY_NAME=Your Company Name
